1. 第一个错误：
text
error: src refspec master does not match any
error: failed to push some refs to 'https://github.com/your-username/your-repo-name.git'
原因：您的本地仓库中没有名为 master 的分支。现在 GitHub 等平台的默认主分支名称是 main，但您的本地仓库可能还没有任何提交。

2. 后续错误：
text
error: remote upstream already exists
原因：您重复执行了 git remote add upstream 命令，但名为 upstream 的远程仓库已经存在。

解决方案
第一步：检查当前状态
在终端中执行以下命令来了解现状：

bash
# 检查所有分支（本地和远程）
git branch -a

# 检查所有远程仓库配置
git remote -v
第二步：解决分支不存在的问题
如果 git branch -a 显示没有任何分支，或者没有 master/main 分支，您需要：

创建初始提交（如果还没有任何提交）：

bash
git add .
git commit -m "初始提交"
检查并创建正确的主分支：

bash
# 查看当前分支
git branch

# 如果显示没有分支或不在主分支，重命名当前分支为 main
git branch -M main

# 或者，如果您想使用 master
# git branch -M master
第三步：推送到远程仓库
现在推送您的代码：

bash
# 如果您的分支名为 main
git push -u origin main

# 或者如果您的分支名为 master  
git push -u origin master
第四步：管理远程仓库配置
由于 upstream 已经存在，您应该：

bash
# 查看现有的 upstream 地址
git remote -v

# 如果需要修改 upstream 的 URL
git remote set-url upstream https://github.com/正确的作者/正确的仓库名.git

# 如果不需要 upstream，可以删除它
git remote remove upstream
完整正确的操作流程
如果您是第一次设置这个仓库，建议按以下顺序操作：

bash
# 1. 检查状态
git branch
git remote -v

# 2. 添加文件并提交（如果还没有提交）
git add .
git commit -m "初始提交"

# 3. 确保分支名称正确（比如使用 main）
git branch -M main

# 4. 推送到 origin
git push -u origin main

# 5. 只在需要时添加 upstream（比如要给开源项目贡献代码）
git remote add upstream https://github.com/原始作者/原始仓库.git
总结
主要问题：您尝试推送一个不存在的 master 分支

解决方法：先创建提交，确认分支名称，然后推送正确的分支

次要问题：重复添加同名的远程仓库

解决方法：使用 set-url 修改或 remove 删除重复的配置

请先执行 git branch 和 git remote -v 命令，告诉我输出结果，我可以给您更精确的下一步指导。